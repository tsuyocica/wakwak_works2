<div class="card mb-4">
  <div class="card-header">プロフィール</div>
  <div class="card-body">
    <p><strong>氏名:</strong> <%= user.full_name %> (<%= user.furigana %>)</p>
    <p><strong>生年月日:</strong> <%= user.birth_date.strftime("%Y年%m月%d日") %></p>
    <p><strong>経験:</strong> <%= user.experience.presence || "未登録" %></p>
    <p><strong>資格:</strong> <%= user.qualification.presence || "未登録" %></p>
    <%= link_to "プロフィールを編集", edit_user_path(@user), class: "btn btn-warning" %>
  </div>
</div>